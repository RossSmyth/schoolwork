function uOut = u1(t)
% U1 Example input for validation of integraiton methods.
%
%   Always zero.
% 
%   Inputs
%      t = Timestep to evaluate the input at.
%
%   Outputs
%       uOut = The input at the evaluated time.
%
uOut = 0;
%uOut = sin(5*t);